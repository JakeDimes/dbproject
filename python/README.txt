This python script provides a simple interface to view queries and views in the database.

The src folder contains queries and should not be tampered with, and should be in the same directory as the interface.py file.

Requires: Python >= 3.10

Insall pip requirments in requirments.txt

Launch the program with the path to the database (your directory may be different):

	$ python interface.py ./example.db
